services:
  db:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_DB: oar
      POSTGRES_USER: oar
      POSTGRES_PASSWORD: oar_dev
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
